<%= simple_form_for [@cocktail, @dose] do |d| %>
  <%= d.input :description, placeholder: "2 spoons" %>
  <%= d.association :ingredient %>
  <%= d.button :submit, class: 'btn btn-prymary btn-lg'%>
<% end %>
